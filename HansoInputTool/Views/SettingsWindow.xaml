<Window x:Class="HansoInputTool.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="設定" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Background="{StaticResource BgBrush}">
        <StackPanel Margin="15">
            <TabControl>
                <!-- 料金設定タブ -->
                <TabItem Header="料金設定">
                    <StackPanel Margin="10">
                        <ItemsControl ItemsSource="{Binding Rates}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding Key}" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,5">
                                        <StackPanel Margin="10">
                                            <Grid Margin="0,3">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="基本料金" VerticalAlignment="Center"/>
                                                <TextBox Grid.Column="1" Text="{Binding Value.BaseFee, UpdateSourceTrigger=PropertyChanged}" MinWidth="150"/>
                                            </Grid>
                                            <Grid Margin="0,3">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="走行料金" VerticalAlignment="Center"/>
                                                <TextBox Grid.Column="1" Text="{Binding Value.MileageFee, UpdateSourceTrigger=PropertyChanged}" MinWidth="150"/>
                                            </Grid>
                                            <Grid Margin="0,3">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="深夜固定" VerticalAlignment="Center"/>
                                                <TextBox Grid.Column="1" Text="{Binding Value.LateNightFixedFee, UpdateSourceTrigger=PropertyChanged}" MinWidth="150"/>
                                            </Grid>
                                            <Grid Margin="0,3">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="深夜単価" VerticalAlignment="Center"/>
                                                <TextBox Grid.Column="1" Text="{Binding Value.LateNightUnitFee, UpdateSourceTrigger=PropertyChanged}" MinWidth="150"/>
                                            </Grid>
                                        </StackPanel>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </TabItem>

                <!-- 列マッピング設定タブ -->
                <TabItem Header="列マッピング設定">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                        <StackPanel Margin="10">
                            <!-- 通常シート -->
                            <GroupBox Header="通常シート（列番号）" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,5">
                                <StackPanel DataContext="{Binding ColumnMap.NormalSheet}" Margin="10">
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="日" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding Day, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="搬送回数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding HansoCount, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="有料キロ" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding YuryoKm, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="無料キロ" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding MuryoKm, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="基本料" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding KihonFee, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="走行料" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding SokoFee, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="深夜料" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding ShinyaFee, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="合計" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding TotalFee, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="深夜時間" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding ShinyaMinutes, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="行旅" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding IsKoryo, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                </StackPanel>
                            </GroupBox>

                            <!-- 東日本シート -->
                            <GroupBox Header="東日本シート（セル番地）" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,5">
                                <StackPanel DataContext="{Binding ColumnMap.EastSheet}" Margin="10">
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="延実働車輌数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding Jitsudo, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="搬送回数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding Hanso, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="有料キロ数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding YuryoKm, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="無料キロ数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding MuryoKm, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="運輸実績" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding UnsoJisseki, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                </StackPanel>
                            </GroupBox>

                            <!-- 集計シート -->
                            <GroupBox Header="集計シート（セル番地）" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="0,5">
                                <StackPanel DataContext="{Binding ColumnMap.ShukeiSheet}" Margin="10">
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="稼働日数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding Days, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="搬送回数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding Hanso, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="有料キロ数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding YuryoKm, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="無料キロ数" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding MuryoKm, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="合計金額" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1" Text="{Binding Total, UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="キャンセル" Background="{StaticResource ButtonSecondaryBgBrush}"
                        Command="{Binding CancelCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,0,10,0" Style="{StaticResource NormalButton}"/>
                <Button Content="保存" Background="{StaticResource GreenBrush}" 
                        Command="{Binding SaveCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource NormalButton}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>