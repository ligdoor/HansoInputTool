<Window x:Class="HansoInputTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:converters="clr-namespace:HansoInputTool.Converters"
        mc:Ignorable="d"
        Title="搬送実績入力ツール" Height="830" Width="850" MinHeight="700" MinWidth="800">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

        <Style x:Key="PlaceholderTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SubTextBrush}"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>
        
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource RedBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </Window.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding OnLoadedCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding OnClosingCommand}" PassEventArgsToCommand="True"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Background="{StaticResource BgBrush}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="搬送実績入力ツール" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding AppVersion}" FontSize="11" VerticalAlignment="Center"
                               Foreground="{StaticResource SubTextBrush}" Margin="8,0,0,0"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding OpenVehicleAnnualSummaryCommand}"
                            Style="{StaticResource NormalButton}"
                            Background="#0F766E"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Padding="12,8"
                            Margin="0,0,8,0">
                        <Button.Effect>
                            <DropShadowEffect Color="#0F766E" 
                                              Opacity="0.4" 
                                              BlurRadius="15" 
                                              ShadowDepth="0"/>
                        </Button.Effect>
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="📈 年度集計"/>
                    </Button>
                    <Button Command="{Binding OpenMonthlyReportDashboardCommand}"
                            Style="{StaticResource NormalButton}"
                            Background="{StaticResource AccentBrush}"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Padding="12,8"
                            Margin="0,0,8,0">
                        <Button.Effect>
                            <DropShadowEffect Color="#F4B942" 
                                              Opacity="0.4" 
                                              BlurRadius="15" 
                                              ShadowDepth="0"/>
                        </Button.Effect>
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="📊 月報統計"/>
                    </Button>
                    <Button Command="{Binding OpenPdfImportCommand}"
                            Style="{StaticResource NormalButton}"
                            Background="#7c3aed"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Padding="12,8"
                            Margin="0,0,8,0"
                            ToolTip="日報PDFを読み込んで自動入力">
                        <Button.Effect>
                            <DropShadowEffect Color="#7c3aed"
                                              Opacity="0.4"
                                              BlurRadius="15"
                                              ShadowDepth="0"/>
                        </Button.Effect>
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="📄 PDF読込"/>
                    </Button>
                    <Button Command="{Binding CreateBackupCommand}" 
                            Style="{StaticResource NormalButton}" Background="Transparent" 
                            Foreground="{StaticResource SubTextBrush}" Margin="0,0,8,0"
                            Padding="8,6"
                            ToolTip="手動でバックアップを作成">
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="💾 バックアップ"/>
                    </Button>
                    <Button Command="{Binding RestoreBackupCommand}" 
                            Style="{StaticResource NormalButton}" Background="Transparent" 
                            Foreground="{StaticResource SubTextBrush}" Margin="0,0,8,0"
                            Padding="8,6"
                            ToolTip="バックアップから復元">
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="📂 復元"/>
                    </Button>
                    <Button Command="{Binding OpenBackupFolderCommand}" 
                            Style="{StaticResource NormalButton}" Background="Transparent" 
                            Foreground="{StaticResource SubTextBrush}" Margin="0,0,8,0"
                            Padding="8,6"
                            ToolTip="バックアップフォルダを開く">
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="📁 フォルダ"/>
                    </Button>
                    <Button Command="{Binding OpenHelpCommand}" 
                            Style="{StaticResource NormalButton}" Background="Transparent" 
                            Foreground="{StaticResource SubTextBrush}" Margin="0,0,8,0"
                            Padding="8,6">
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="❔ HELP"/>
                    </Button>
                    <Button Command="{Binding OpenSettingsCommand}" 
                            Style="{StaticResource NormalButton}" Background="Transparent" 
                            Foreground="{StaticResource SubTextBrush}"
                            Padding="8,6">
                        <TextBlock FontFamily="Segoe UI Emoji, Segoe UI" Text="⚙️ 設定"/>
                    </Button>
                </StackPanel>
            </DockPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,0,8,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Margin="8,6">
                            <Label Content="期" Padding="0" VerticalAlignment="Center"/>
                            <Grid Margin="6,0,0,0">
                                <TextBox x:Name="PeriodTextBox" Text="{Binding Period, UpdateSourceTrigger=PropertyChanged}" Width="50" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                            <Setter Property="Text" Value="例:46"/>
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=PeriodTextBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style></TextBlock>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,0,8,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Margin="8,6">
                            <Label Content="月" Padding="0" VerticalAlignment="Center"/>
                            <Grid Margin="6,0,0,0">
                                <TextBox x:Name="MonthTextBox" Text="{Binding Month, UpdateSourceTrigger=PropertyChanged}" Width="40" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                            <Setter Property="Text" Value="例:04"/>
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=MonthTextBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style></TextBlock>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,0,8,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Margin="8,6">
                            <Label Content="R" Padding="0" VerticalAlignment="Center"/>
                            <Grid Margin="6,0,0,0">
                                <TextBox x:Name="RNumberTextBox" Text="{Binding RNumber, UpdateSourceTrigger=PropertyChanged}" Width="40" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                            <Setter Property="Text" Value="例:1"/>
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=RNumberTextBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style></TextBlock>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TabControl Grid.Column="0" Margin="0,0,10,0" SelectedIndex="{Binding SelectedTabIndex}">
                    <TabItem Header="通常シート">
                        <StackPanel Margin="10" DataContext="{Binding NormalSheet}">
                            <ComboBox ItemsSource="{Binding NormalSheets}" SelectedItem="{Binding SelectedNormalSheet}" Margin="0,0,0,10"/>
                            
                            <!-- 日(B) -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="日(B)" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="NormalDayTextBox" Text="{Binding Day, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:5"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=NormalDayTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding DayError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding DayError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 有料キロ(D) -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="有料キロ(D)" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="NormalYuryoKmTextBox" Text="{Binding YuryoKm, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:120.5"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=NormalYuryoKmTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding YuryoKmError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding YuryoKmError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 無料キロ(E) -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="無料キロ(E)" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="NormalMuryoKmTextBox" Text="{Binding MuryoKm, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:10"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=NormalMuryoKmTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding MuryoKmError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding MuryoKmError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 深夜料金(H)/深夜時間(K) -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="深夜料金(H)" Padding="0,0,0,2" Visibility="{Binding IsOotsukiSheet, Converter={StaticResource BoolToVis}}"/>
                                        <Label Content="深夜時間(K)" Padding="0,0,0,2" Visibility="{Binding IsOotsukiSheet, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"/>
                                        <Grid>
                                            <TextBox x:Name="NormalLateValueTextBox" Text="{Binding LateValue, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="LastNormalTextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:90"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <!-- TextBoxが空 かつ 大月シートでない（深夜時間モード）のときだけ表示 -->
                                                            <MultiDataTrigger>
                                                                <MultiDataTrigger.Conditions>
                                                                    <Condition Binding="{Binding Text, ElementName=NormalLateValueTextBox}" Value=""/>
                                                                    <Condition Binding="{Binding IsOotsukiSheet}" Value="False"/>
                                                                </MultiDataTrigger.Conditions>
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </MultiDataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:3000"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <!-- TextBoxが空 かつ 大月シート（深夜料金モード）のときだけ表示 -->
                                                            <MultiDataTrigger>
                                                                <MultiDataTrigger.Conditions>
                                                                    <Condition Binding="{Binding Text, ElementName=NormalLateValueTextBox}" Value=""/>
                                                                    <Condition Binding="{Binding IsOotsukiSheet}" Value="True"/>
                                                                </MultiDataTrigger.Conditions>
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </MultiDataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding LateValueError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding LateValueError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <ItemsControl ItemsSource="{Binding FlagItems}" Margin="0,10,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding DisplayName}"
                                                  IsChecked="{Binding IsChecked}"
                                                  Margin="5,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Content="登録" Command="{Binding RegisterNormalCommand}" HorizontalAlignment="Right" Style="{StaticResource NormalButton}" Height="22" Width="38" Margin="0,40,125,0"/>
                        </StackPanel>
                    </TabItem>
                    
                    <TabItem Header="東日本シート">
                        <StackPanel Margin="10" DataContext="{Binding EastSheet}">
                            <ComboBox ItemsSource="{Binding EastSheets}" SelectedItem="{Binding SelectedEastSheet}" Margin="0,0,0,10"/>
                            
                            <!-- 延実働車輌数 -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="延実働車輌数" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="EastJitsudoTextBox" Text="{Binding Jitsudo, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:12"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=EastJitsudoTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding JitsudoError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding JitsudoError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 搬送回数 -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="搬送回数" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="EastHansoTextBox" Text="{Binding Hanso, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:34"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=EastHansoTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding HansoError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding HansoError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 有料キロ数 -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="有料キロ数" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="EastYuryoKmTextBox" Text="{Binding YuryoKm, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:200.0"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=EastYuryoKmTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding YuryoKmError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding YuryoKmError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 無料キロ数 -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="無料キロ数" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="EastMuryoKmTextBox" Text="{Binding MuryoKm, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="TextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:15"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=EastMuryoKmTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding MuryoKmError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding MuryoKmError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- 運輸実績 -->
                            <Border BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="2" CornerRadius="3" Margin="0,5">
                                <Grid>
                                    <StackPanel Margin="5">
                                        <Label Content="運輸実績" Padding="0,0,0,2"/>
                                        <Grid>
                                            <TextBox x:Name="EastUnsoTextBox" Text="{Binding Unso, UpdateSourceTrigger=PropertyChanged}" Padding="5,2" BorderThickness="0" Background="Transparent" KeyDown="LastEastTextBox_KeyDown"/>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextBlockStyle}">
                                                        <Setter Property="Text" Value="例:125000"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, ElementName=EastUnsoTextBox}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock Text="{Binding UnsoError}" 
                                                   Style="{StaticResource ErrorTextStyle}"
                                                   Visibility="{Binding UnsoError, Converter={StaticResource StringToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <DockPanel Margin="0,10,0,0">
                                <TextBlock Text="{Binding SheetStatus}" VerticalAlignment="Center" Margin="5,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource SubTextBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSheetRegistered}" Value="True">
                                                    <Setter Property="Foreground" Value="{StaticResource GreenBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Button Content="登録" Command="{Binding RegisterEastCommand}" Style="{StaticResource NormalButton}" Height="22" Width="45"/>
                            </DockPanel>
                        </StackPanel>
                    </TabItem>
                </TabControl>
                
                <Grid Grid.Column="1">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="1">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DataGrid ItemsSource="{Binding PreviewDataView}" AutoGenerateColumns="False" SelectedItem="{Binding SelectedRow}" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
                        <DataGrid.Resources>
                            <converters:BindingProxy x:Key="Proxy" Data="{Binding}"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="日" Binding="{Binding B_Day}" Width="50"/>
                            <DataGridTextColumn Header="搬送" Binding="{Binding C_Hanso}" Width="50"/>
                            <DataGridTextColumn Header="有料km" Binding="{Binding D_YuryoKm, StringFormat={}{0:N0}}" Width="*"/>
                            <DataGridTextColumn Header="無料km" Binding="{Binding E_MuryoKm, StringFormat={}{0:N0}}" Width="*"/>
                            <DataGridTextColumn Header="フラグ" Binding="{Binding FlagSummaryText}" Width="100"/>
                            <DataGridTextColumn Header="深夜料金(円)" Binding="{Binding H_LateFeeOotsuki}" Width="*" Visibility="{Binding Data.NormalSheet.IsOotsukiSheet, Source={StaticResource Proxy}, Converter={StaticResource BoolToVis}}"/>
                            <DataGridTextColumn Header="深夜時間(分)" Binding="{Binding K_LateMinutes}" Width="*" Visibility="{Binding Data.NormalSheet.IsOotsukiSheet, Source={StaticResource Proxy}, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="選択行を編集" Command="{Binding EditRowCommand}" Margin="0,0,5,0" Style="{StaticResource NormalButton}"/>
                        <Button Content="選択行を削除" Command="{Binding DeleteRowCommand}" Background="{StaticResource RedBrush}" Style="{StaticResource NormalButton}" Height="25"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                         Background="{StaticResource PanelBackgroundBrush}"/>
                <DockPanel Grid.Row="1" Margin="0,10,0,0">
                    <Button Content="転記実行" DockPanel.Dock="Right" Background="{StaticResource GreenBrush}" Command="{Binding TransferCommand}" Style="{StaticResource NormalButton}" Height="25" Width="60"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="実績月報を読み込む" Background="#4f46e5" Command="{Binding LoadGeppoFileCommand}" Style="{StaticResource NormalButton}"/>
                        <Button Content="入力内容を保存" Background="{StaticResource ButtonSecondaryBgBrush}" Command="{Binding SaveInputCommand}" Margin="5,0,0,0" Style="{StaticResource NormalButton}"/>
                    </StackPanel>
                </DockPanel>
            </Grid>

        </Grid>
    </Grid>
</Window>
