<Window x:Class="HansoInputTool.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="転記処理の進捗" Height="250" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <!-- ↓↓↓ ルート要素のGridに、背景色を強制的に設定 ↓↓↓ -->
    <Grid Background="{StaticResource BgBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding StatusText}" FontSize="14" Margin="0,0,0,5"/>
            <ProgressBar Grid.Row="1" Height="15" Value="{Binding ProgressValue}"/>
            <TextBlock Grid.Row="2" Text="{Binding ProgressPercentage}" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,5,0,10"/>
            <TextBox Grid.Row="3" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="{StaticResource PanelBackgroundBrush}"/>
            <Button Grid.Row="4" Content="閉じる" HorizontalAlignment="Right" Margin="0,10,0,0"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    IsEnabled="{Binding IsCloseButtonEnabled}" Style="{StaticResource NormalButton}"/>
        </Grid>
    </Grid>
</Window>